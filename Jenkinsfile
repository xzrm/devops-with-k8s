pipeline {
    agent any
    stage {
        stage("Building Docker Image")
            steps{
                echo "Hello world"
                // sh "cd main"
                // sh "docker build -t xzarem/main:main"
            }
    }

}